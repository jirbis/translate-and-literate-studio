# Sample Technical Text (EN)

The orchestrator splits the document into chunks, applies glossary rules, and executes each agent step in order.
Consistency is enforced through the terminology manager and QA checks.

// Orchestrator stub
// Coordinates pipeline steps: preprocess, terminology, translate, literate, QA, learn.
export function runPipeline() {
  throw new Error('orchestrator.mjs is a reference stub. Implement pipeline execution logic.');
}
